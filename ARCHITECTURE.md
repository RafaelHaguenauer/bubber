# 🏗️ Diagrama da Arquitetura - Bubber

## Visão Geral do Sistema

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         CLIENTE (Web/Mobile)                             │
└────────────────────────┬────────────────────────────────────────────────┘
                         │
                         │ HTTP/WebSocket
                         │
        ┌────────────────▼────────────────────┐
        │     API Gateway / GraphQL Server    │
        │         (Node.js + Apollo)          │
        │    :4000                            │
        ├─────────────────────────────────────┤
        │ • Schema GraphQL                    │
        │ • Resolvers                         │
        │ • Autenticação                      │
        │ • Rate Limiting                     │
        └────┬─────────────────────────┬──────┘
             │                         │
             │ gRPC                    │ SQL
             │ Port 50051              │ Port 5432
             │                         │
     ┌───────▼─────────┐      ┌────────▼──────────┐
     │   Microserviço  │      │   PostgreSQL      │
     │   Motoristas    │      │   Banco de Dados  │
     │  (Python/gRPC)  │      │                   │
     │  :50051         │      │ • Usuários        │
     ├─────────────────┤      │ • Motoristas      │
     │ • Busca geo     │      │ • Veículos        │
     │ • Caching local │      │ • Corridas        │
     │ • Haversine     │      │ • Pagamentos      │
     └─────────────────┘      └───────────────────┘
             ▲
             │ gRPC
             │ Port 50052
             │
     ┌───────┴─────────┐
     │  Microserviço   │
     │   Roteamento    │
     │  (Python/gRPC)  │
     │  :50052         │
     ├─────────────────┤
     │ • OSRM API      │──────────┐
     │ • Otimização    │          │ HTTP
     │ • Turn-by-turn  │          │ Port 5000
     └─────────────────┘          │
                                  │
                         ┌────────▼──────────┐
                         │   OSRM Backend    │
                         │  (Container)      │
                         │                   │
                         │ • Dados OSM       │
                         │ • Roteamento      │
                         │ • Matriz distância│
                         └───────────────────┘
```

## Fluxo de uma Corrida

```
┌─────────────┐
│   Usuário   │
└──────┬──────┘
       │
       │ 1. Solicitar Corrida
       │    (Latitude/Longitude origem e destino)
       ▼
┌──────────────────────┐
│   API GraphQL        │
│ mutation criarCorrida│
└──────┬───────────────┘
       │
       │ 2. Calcular Rota
       │    (Chamar via gRPC)
       ▼
┌──────────────────────┐
│ Microserviço         │
│ Roteamento (gRPC)    │
└──────┬───────────────┘
       │
       │ 3. Chamar OSRM
       ▼
┌──────────────────────┐
│ OSRM                 │
│ (Dados OSM)          │
└──────┬───────────────┘
       │
       │ 4. Retornar: distância + duração
       ▼
┌──────────────────────┐
│ API GraphQL          │
│ (Salvar no DB)       │
│ + Valor calculado    │
└──────┬───────────────┘
       │
       │ 5. Buscar Motoristas Próximos
       │    (Chamar via gRPC)
       ▼
┌──────────────────────┐
│ Microserviço         │
│ Motoristas (gRPC)    │
└──────┬───────────────┘
       │
       │ 6. Consultar DB + Haversine
       ▼
┌──────────────────────┐
│ PostgreSQL           │
│ Dados Motoristas     │
└──────┬───────────────┘
       │
       │ 7. Retornar motoristas ordenados por distância
       ▼
┌──────────────────────┐
│ API GraphQL          │
│ motoristasPróximos   │
└──────┬───────────────┘
       │
       │ 8. Retornar para cliente
       ▼
┌──────────────┐
│   Usuário    │
│ Aceita       │
│ se quiser    │
└──────┬───────┘
       │
       │ 9. Confirmar Motorista
       │    selecionarMotoristaParaCorrida()
       ▼
┌──────────────────────┐
│ API GraphQL          │
│ Atualizar status +   │
│ Marcar motorista     │
│ como indisponível    │
└──────┬───────────────┘
       │
       │ 10. Notificar Motorista
       │     (via WebSocket ou Push)
       ▼
┌──────────────┐
│   Motorista  │
│ Aceita/       │
│ Recusa        │
└──────────────┘
```

## Comunicação entre Microsserviços

```
┌─────────────────────────────────────────┐
│          API GraphQL (Node.js)          │
└─────────────┬──────────────────┬────────┘
              │                  │
       gRPC   │                  │   gRPC
              │                  │
    ┌─────────▼──────────┐   ┌───▼──────────────┐
    │ Motoristas Service │   │Roteamento Service│
    │   (Python)         │   │   (Python)       │
    │   Port: 50051      │   │   Port: 50052    │
    └─────────┬──────────┘   └───┬──────────────┘
              │                  │
              │ SQL              │ HTTP
              │                  │
        ┌─────▼──────────┐   ┌───▼──────────────┐
        │  PostgreSQL    │   │   OSRM Backend   │
        │                │   │   Port: 5000     │
        │ Tabelas:       │   │                  │
        │ • Motorista    │   │ Dados:           │
        │ • Usuario      │   │ • OSM            │
        │ • Veiculo      │   │ • Índices        │
        │ • Corrida      │   │ • Grafos         │
        │ • Pagamento    │   │                  │
        └────────────────┘   └──────────────────┘
```

## Fluxo de Dados em cada Camada

### Layer 1: Apresentação
```
┌──────────────────────────────────┐
│         Aplicação Cliente         │
│  • Web (React, Vue, Angular)      │
│  • Mobile (React Native, Flutter) │
│  • GraphQL Queries/Mutations      │
└────────────────┬─────────────────┘
                 │
            HTTP │
                 ▼
```

### Layer 2: API Gateway
```
┌──────────────────────────────────┐
│      Apollo GraphQL Server       │
│     Node.js + TypeScript         │
│                                  │
│ • Parse GraphQL                  │
│ • Validar Entrada                │
│ • Autenticar                     │
│ • Rate Limit                     │
│ • Chamar Resolvers               │
└────────────────┬─────────────────┘
                 │
          gRPC   │  SQL
                 │
```

### Layer 3: Microsserviços & Banco
```
┌──────────────────────────────────┐
│      Microsserviços             │
│  (Python + FastAPI)              │
│                                  │
│ • Lógica de Negócio              │
│ • Consultas ao Banco             │
│ • Chamadas Externas (OSRM)       │
└──────────────────────────────────┘
```

### Layer 4: Dados & Roteamento
```
┌──────────────────────────────────┐
│   PostgreSQL | OSRM              │
│   Persistência | Roteamento      │
└──────────────────────────────────┘
```

## Escalabilidade & Redundância

Para produção, a arquitetura pode ser escalada:

```
                    ┌─────────────────┐
                    │  Load Balancer  │
                    │    (Nginx/HAProxy)
                    └────────┬────────┘
         ┌──────────────────┼──────────────────┐
         │                  │                  │
    ┌────▼─────┐      ┌─────▼────┐      ┌────▼─────┐
    │  GraphQL │      │ GraphQL  │      │ GraphQL  │
    │ Instance1│      │ Instance2│      │ Instance3│
    └────┬─────┘      └────┬─────┘      └────┬─────┘
         │                 │                  │
         │            ┌────▼────┐             │
         └───────────►│  Database Master     │◄─────┘
                      │  (Primary)           │
                      └─────┬────────────────┘
                            │
                      ┌─────▼────┐
                      │  Database │
                      │  Replica  │
                      │(Read-only)│
                      └───────────┘

         ┌─────────────────────────────────┐
         │    Microserviços (redundantes)  │
         │                                 │
    ┌────▼─────┐       ┌────────┐     ┌────▼─────┐
    │Motoristas│       │gRPC LB │     │Motoristas│
    │Instance1 │───────┤ (Nginx)├─────│Instance2 │
    └──────────┘       └────────┘     └──────────┘
    
    ┌──────────────┐   ┌────────┐   ┌──────────────┐
    │Roteamento    │   │gRPC LB │   │Roteamento    │
    │Instance1     │───┤(Nginx) ├───│Instance2     │
    └──────────────┘   └──┬─────┘   └──────────────┘
                          │
                    ┌─────▼─────┐
                    │  OSRM     │
                    │ (Cluster) │
                    └───────────┘
```

## Ambiente Docker

```
docker-compose.yml
├── postgres              (Banco: 5432)
├── osrm                  (Roteamento: 5000) [Opcional]
├── api-graphql           (API: 4000)
├── service-motoristas    (gRPC: 50051)
└── service-roteamento    (gRPC: 50052)

Volumes:
├── postgres_data        (Persistência do Banco)
└── osrm-data           (Dados OSM)

Network: bubber-network (bridge)
```

## Segurança

```
┌─────────────────────────────────────┐
│       Internet/Cliente              │
└────────────────┬────────────────────┘
                 │
            HTTPS│ TLS/SSL
                 ▼
         ┌───────────────┐
         │   Firewall    │
         │  (Port 443)   │
         └───────┬───────┘
                 │
            HTTP │
                 ▼
         ┌───────────────┐
         │ Load Balancer │
         │  (WAF, DDoS)  │
         └───────┬───────┘
                 │
                 ▼
         ┌──────────────────┐
         │  API GraphQL     │
         │  • Auth JWT      │
         │  • Rate Limit    │
         │  • Validação     │
         └────────┬─────────┘
                  │
            gRPC  │ (Seguro: mTLS em produção)
                  ▼
         ┌──────────────────┐
         │ Microsserviços   │
         │  (Private Net)   │
         └─────────┬────────┘
                   │
                   ▼
         ┌──────────────────┐
         │  Banco de Dados  │
         │  (Private Net)   │
         │  Criptografado   │
         └──────────────────┘
```

---

**Diagrama atualizado:** Janeiro 2024
